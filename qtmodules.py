"""
This module contains all the Qt modules that are used in the application.
We should create all the widgets in this module and then import them in the other files.
"""
from PyQt6.QtCore import Qt, QDate, pyqtSignal, QPointF
from PyQt6.QtWebEngineWidgets import QWebEngineView
from PyQt6.QtCore import QUrl, pyqtSlot, QRect, Qt, QTimer
from PyQt6.QtWidgets import QSizePolicy, QSplashScreen, QListWidgetItem, QAbstractItemView, QApplication, QComboBox, QDateEdit, QHBoxLayout, QLabel, QLineEdit, QTextEdit, QVBoxLayout, QWidget, QPushButton, QCompleter, QListWidget, QMainWindow, QTabWidget
from PyQt6.QtWebChannel import QWebChannel
from PyQt6.QtGui import QPixmap, QIcon, QColor, QFont, QLinearGradient, QPainter, QPen, QBrush, QMovie
